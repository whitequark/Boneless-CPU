
HEADER COLD
    MOVL PSP,8 
    MOVL RSP,16
NEXT

HEADER DOCOL ; run the do colon code
    NOP
NEXT


HEADER ?key
    STX SP,SP,1
NEXT

HEADER pad
.alloc pad_alloc,10
NEXT

HEADER DUP
    MOV W,TOS
    push 
NEXT

HEADER QUIT
    LDX W,SP,0
NEXT

HEADER &
NEXT

HEADER @
NEXT

HEADER !
NEXT

HEADER +
    pop
    ADD W,TOS,W
    MOV TOS,W 
NEXT 

HEADER BRANCH
NEXT

HEADER SWAP
    pop
    XCHG W,TOS
    push
NEXT

HEADER DROP
    pop
NEXT

